apiVersion: 1

datasources:
  - name: CSV-cut-data
    type: marcusolsson-csv-datasource
    access: proxy
    url: "/data/cut-data.csv"
    isDefault: false
    basicAuth: false
    editable: true
    jsonData:
      storage: "local"
