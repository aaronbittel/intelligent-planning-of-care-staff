apiVersion: 1

datasources:
  - name: CSV-latest_random_forest
    type: marcusolsson-csv-datasource
    access: proxy
    url: "/data/random_forest/latest.csv"
    isDefault: false
    basicAuth: false
    editable: true
    jsonData:
      storage: "local"
