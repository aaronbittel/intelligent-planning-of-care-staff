apiVersion: 1

datasources:
  - name: CSV-cut-data
    type: marcusolsson-csv-datasource
    access: proxy
    url: "/data/latest_history.csv"
    isDefault: false
    basicAuth: false
    editable: true
    jsonData:
      storage: "local"
