apiVersion: 1

datasources:
  - name: CSV-sarima_last_test
    type: marcusolsson-csv-datasource
    access: proxy
    url: "/data/sarima/latest.csv"
    isDefault: false
    basicAuth: false
    editable: true
    jsonData:
      storage: "local"
