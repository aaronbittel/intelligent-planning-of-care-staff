apiVersion: 1

datasources:
  - name: CSV-random-forest
    type: marcusolsson-csv-datasource
    access: proxy
    url: "/data/random-forest.csv"
    isDefault: false
    basicAuth: false
    editable: true
    jsonData:
      storage: "local"
